<h1>Listing users</h1>

<table>
  <tr>
    <th>Forename</th>
    <th>Surename</th>
    <th>Student number</th>
    <th>Email</th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.forename %></td>
    <td><%= user.surename %></td>
    <td><%= user.student_number %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
